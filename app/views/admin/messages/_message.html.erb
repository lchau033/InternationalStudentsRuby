<!--Facebook like chat _message.html.erb-->
<!--CREDITS: https://www.linkedin.com/pulse/facebook-like-chat-application-using-ruby-rails-kanhaiya-kumar?articleId=8194060515700978927-->

<li class="<%=  self_or_other(message) %>">
  <div class="avatar">
    <% if message.user.image_url %>
        <%= image_tag message.user.image_url, class: ''  if message.user %>
    <% else %>
        <div class="">
        <span class="initials">
          <%= message.user.name.chr.upcase %>
        </span>
        </div>
    <% end %>
  </div>
  <div class="chatboxmessagecontent">
    <p><%= message.body %></p>
    <time datetime="<%= message.created_at %>" title="<%= message.created_at.strftime("%d %b  %Y at %I:%M%p") %>">
      <%= message_interlocutor(message).name %> • <%= message.created_at.strftime("%H:%M %p") %>
    </time>
  </div>
</li>